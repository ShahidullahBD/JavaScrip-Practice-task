const computer = {
    brand: 'MS',
    origin: 'USA',
    price: 30000,
    monitor: 'hp'
}
// console.log(computer);

const keys = Object.keys(computer);
console.log(keys);
const values = Object.values(computer);
console.log(values);